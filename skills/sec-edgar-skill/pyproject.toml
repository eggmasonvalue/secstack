[project]
name = "secstack-sec-edgar"
version = "0.1.0"
description = "SEC EDGAR data scripts for SecStack"
requires-python = ">=3.11"
dependencies = [
    "edgartools>=5",
    "pandas>=2",
    "truststore>=0.9",
]

[tool.uv]
package = false
