[project]
name = "secstack-market-scout"
version = "0.1.0"
description = "Market data scripts for SecStack"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2",
    "tabulate>=0.9",
    "truststore>=0.9",
    "yfinance>=0.2",
]

[tool.uv]
package = false
